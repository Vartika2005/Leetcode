class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        unordered_set<int> s;
        int ans;
        for(int num : nums) {
            if(s.count(num))
             ans = num;
            s.insert(num);
        }
        return ans;
    }
};
