class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {  //using hash set
        unordered_set<int> seen;
        for(int num : nums) {
            if(seen.count(num)) return true;  //if duplicate element found immediately return true
            seen.insert(num);
        }
        return false;
    }
};
